# Core
fastapi==0.135.2
uvicorn[standard]==0.46.0
PyYAML==6.0.3

# HTTP
aiohttp==3.14.1

# Database
aiosqlite==0.22.1
asyncpg>=0.31.0

# Monitoring
prometheus-client==0.25.0

# Security & Identity
cryptography==48.0.1
PyJWT==2.13.0

# Export & Observability
aiofiles==25.1.0
sentry-sdk[fastapi]==2.61.1

# Observability
opentelemetry-api>=1.25.0
opentelemetry-sdk>=1.25.0
opentelemetry-instrumentation>=0.64b0
opentelemetry-instrumentation-fastapi>=0.46b0
opentelemetry-instrumentation-aiohttp-client>=0.63b1
opentelemetry-instrumentation-aiohttp-server>=0.63b1
opentelemetry-exporter-otlp>=1.25.0

# Utilities
cachetools==7.1.4

# Type stubs
types-PyYAML>=6.0.0
types-cachetools>=5.0.0
types-aiofiles>=24.1.0

# Testing
pytest>=9.1.1
pytest-asyncio>=0.23.0

# Optional: Secrets manager (falls back to env vars without this)
# infisical-sdk>=3.0.0

# Optional: NLP-powered PII detection (default masker)
# presidio-analyzer
# presidio-anonymizer

# Optional: ONNX PII masking — OpenAI Privacy Filter NER (opt-in plugin,
# contributed by llmproxy-extended). Enable "ONNX PII Masker" in
# plugins/manifest.yaml and pre-download the model:
#   huggingface-cli download openai/privacy-filter
# onnxruntime>=1.18.0
# numpy>=1.24.0
# transformers>=4.40.0
# huggingface-hub>=0.20.0
# tokenizers>=0.19.0
# Optional GPU on AMD/Intel iGPU (Windows only, int8 variant): onnxruntime-directml

# Optional: WASM plugin support
# extism

# Optional: Parquet export
# pyarrow

# Distributed HA rate limiting & circuit breakers
redis>=5.0.0
