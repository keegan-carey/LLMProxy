# Core
fastapi==0.135.2
uvicorn[standard]==0.46.0
PyYAML==6.0.3

# HTTP
aiohttp==3.14.1

# Database
aiosqlite==0.22.1
asyncpg>=0.29.0

# Monitoring
prometheus-client==0.25.0

# Security & Identity
cryptography==46.0.7
PyJWT==2.13.0

# Export & Observability
aiofiles==25.1.0
sentry-sdk[fastapi]==2.61.1

# Observability
opentelemetry-api>=1.25.0
opentelemetry-sdk>=1.25.0
opentelemetry-instrumentation>=0.46b0
opentelemetry-instrumentation-fastapi>=0.46b0
opentelemetry-instrumentation-aiohttp-client>=0.63b1
opentelemetry-instrumentation-aiohttp-server>=0.63b1
opentelemetry-exporter-otlp>=1.25.0

# Utilities
cachetools==7.1.4

# Type stubs
types-PyYAML>=6.0.0
types-cachetools>=5.0.0
types-aiofiles>=24.1.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Optional: Secrets manager (falls back to env vars without this)
# infisical-sdk>=3.0.0

# Optional: NLP-powered PII detection
# presidio-analyzer
# presidio-anonymizer

# Optional: WASM plugin support
# extism

# Optional: Parquet export
# pyarrow

# Distributed HA rate limiting & circuit breakers
redis>=5.0.0
