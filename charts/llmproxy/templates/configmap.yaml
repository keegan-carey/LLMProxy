apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "llmproxy.fullname" . }}-config
  labels:
    {{- include "llmproxy.labels" . | nindent 4 }}
data:
  config.yaml: |
{{ .Values.config | indent 4 }}
